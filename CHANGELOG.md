
# 1.2.0

2026.08.19

- 修复合并svg插入wps等文档显示黑色不是彩色的问题
- 修复 Gerber 层过滤遗漏 DocumentLayer 等标准层的问题

# 1.1.0

2026.08.18

- 新增 **导出当前板子为 SVG（自定义）...** 菜单：支持勾选要导出的 Gerber 层、为每层设置水平镜像、选择合并为一个 SVG 或独立导出每层 SVG。
- 自定义导出弹窗的层列表显示原始 Gerber 文件名（去掉 `Gerber_` 前缀），导出层与镜像表头支持一键全选/全不选。
- 合并导出时直接输出单个 SVG 文件 `SVG_<板子名>_<PCB文件名>_Merged.svg`，不再打包 ZIP。
- 修复合并导出时所有层颜色均为黑色的问题，各层仍使用 PCB 层表对应颜色。
- 铜层 SVG 节点自动标注 `net` 属性（焊盘、走线、过孔、铺铜区域），通过画布命中与铺铜自校准聚类匹配网络。
- 导出的 ZIP 文件名改为 `SVG_<板子名>_<PCB文件名>.zip`。

# 1.0.0

2026.07.30

- 首个版本