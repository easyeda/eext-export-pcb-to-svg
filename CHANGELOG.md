# 1.1.0

2026.08.18

- 新增 **Export Current Board to SVG (Custom)...** 菜单：支持勾选要导出的 Gerber 层、为每层设置水平镜像、选择合并为一个 SVG 或独立导出每层 SVG。
- 铜层 SVG 节点自动标注 `net` 属性（焊盘、走线、过孔、铺铜区域），通过画布命中与铺铜自校准聚类匹配网络。
- 导出的 ZIP 文件名改为 `SVG_<板子名>_<PCB文件名>.zip`。

# 1.0.0

2026.07.30

- 首个版本